This is a backup file from SQL Server 2005 (SP2). 
Put the file on the same machine as your SQL Server instance, then restore the database.
Make sure you double-check the file paths for the mdf and log files. 